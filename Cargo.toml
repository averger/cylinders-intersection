[package]
name = "cylinders-intersection"
version = "0.1.0"
edition = "2021"
description = "Compute the developed (gueule de loup) patterns from cylinder-cylinder and cylinder-plane intersections, exposed via an Axum HTTP API."
license = "MIT"

[dependencies]
axum = { version = "0.7", features = ["macros"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "signal", "net"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["fs", "trace", "cors"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
nalgebra = "0.33"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1"
thiserror = "1"
clap = { version = "4", features = ["derive"] }
rust-embed = { version = "8", features = ["interpolate-folder-path"] }
mime_guess = "2"

[profile.release]
lto = "thin"
codegen-units = 1
opt-level = 3
strip = "symbols"
